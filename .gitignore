# Dependencias
/node_modules
/.pnp
.pnp.*

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Instrucciones para agentes de IA que genera `next dev` en cada arranque. Se regeneran solas,
# así que versionarlas solo añadiría ruido; lo que el proyecto quiera contarle a un agente va
# en un fichero propio, escrito a mano.
/AGENTS.md
/CLAUDE.md

# Producción
/build

# Tests
/coverage
/playwright-report
/test-results

# Variables de entorno (solo se versiona .env.example)
.env*
!.env.example

# Logs y sistema
npm-debug.log*
.DS_Store
Thumbs.db

# Despliegue
.vercel
.claude/launch.json
